package lucis.compiler.entity;

public enum Tag {
    LINE_BREAK,
    END,

    EXPORT,
    IMPORT,
    CLASS,
    TRAIT,

    INTEGER,
    DECIMAL,
    COMMENT,
    STRING,

    IF,
    ELSE,
    WHILE,

    COMMA,
    DOT,
    COLON,
    QUESTION,
    LESS,
    EQUAL,
    GREATER,
    AND,
    OR,
    NOT,
    XOR,
    PLUS,
    MINUS,
    MULTIPLY,
    DIVIDE,
    REMAINDER,
    LEFT_PARENTHESIS,
    RIGHT_PARENTHESIS,
    LEFT_BRACKET,
    RIGHT_BRACKET,
    LEFT_BRACE,
    RIGHT_BRACE,

    IDENTIFIER,
}
